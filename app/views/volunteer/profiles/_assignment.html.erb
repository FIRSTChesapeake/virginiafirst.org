<span id="assignment_<%= assignment.id %>">
  <%= assignment.position.role %>
  <% if can? :delete, assignment %>
    <%= link_to '<i class="icon-remove"></i>'.html_safe,
                assignment_path(assignment),
                method: :delete,
                confirm: "Are you sure?" %>
  <% end %>
  <br>
</span>


<div class="row">
  <div class="span4">
    <ul class="nav nav-tabs">
      <li class="active"><%= link_to "Sign Up", new_user_registration_path %></li>
      <li><%= link_to "Sign In", new_user_session_path %></li>
    </ul>
    <p>
      Sign up using your Google identity and skip the form below. No need to
      remember a separate password!
    </p>

    <div class="center">
      <%= link_to user_omniauth_authorize_path(:google_oauth2, origin: volunteer_registration_path) do %>
        <%= image_tag "Red-signin_Long_base_44dp.png",
                      mouseover: "Red-signin_Long_hover_44dp.png",
                      alt: "Sign in with Google" %>
      <% end %>
    </div>

    <h4 class="center"> Or </h4>

    <hr>

      <%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
        <%= f.error_notification %>

        <div class="form-inputs">
          <%= f.input :email, :required => true, autofocus: true %>
          <% if f.object.password_required? %>
              <%= f.input :password, :required => true %>
              <%= f.input :password_confirmation, :required => true %>
          <% end %>
        </div>

        <div class="form-actions">
          <%= f.button :submit, "Sign up", class: "btn-primary" %>
        </div>
      <% end %>

      <%= render "users/shared/links" %>
  </div>

  <div class="offset1 span6">
    <h1>Sign Up</h1>
    <p>
      Creating a <%= virginia_first %> account lets you keep track of your
      volunteer preferences and commitments. It's easier than ever to update your
      information or volunteer to more events without having to resubmit the same
      forms over again.
    </p>

    <p>
      If you already signed up with us before this season, then you can simply
      <%= link_to "sign in", new_user_session_path %> to update your preferences
      or volunteer for new events.
    </p>
  </div>
</div>
